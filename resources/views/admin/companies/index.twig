{% extends "theme::layouts/default" %}

{% block content %}
    <h1>Firmalar</h1>

    <table class="table">
        <thead>
        <tr>
            <th>ID</th>
            <th>Firma Adı</th>
            <th>Logo</th>
        </tr>
        </thead>
        <tbody>
        {% for company in companies %}
            <tr>
                <td>{{ company.id }}</td>
                <td>{{ company.name }}</td>
                <td>
                    {% if company.logo %}
                        <img src="{{ company.logo.path }}" alt="Logo" width="100">
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
